spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: some-url

eureka:
  client:
    enabled: false