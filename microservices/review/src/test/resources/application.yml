server.error.include-message: always

spring.cloud.stream.defaultBinder: rabbit

spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false